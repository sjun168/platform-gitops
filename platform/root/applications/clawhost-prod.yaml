apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: clawhost-prod
  namespace: argocd
spec:
  project: apps
  sources:
    - repoURL: https://github.com/sjun168/clawhost.git
      targetRevision: dev
      path: deploy/helm/clawhost
      helm:
        valueFiles:
          - $values/clusters/prod/apps/clawhost/values.yaml
    - repoURL: https://github.com/sjun168/platform-gitops.git
      targetRevision: main
      ref: values
  destination:
    server: https://kubernetes.default.svc
    namespace: app-clawhost-prod
  syncPolicy:
    automated:
      selfHeal: true
      prune: false
    syncOptions:
      - CreateNamespace=true
