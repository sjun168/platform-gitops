apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: aiclaw-secrets
  namespace: app-aiclaw-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: SecretStore
    name: vault-backend
  target:
    name: aiclaw-secrets
  dataFrom:
    - extract:
        key: dev/app-aiclaw/app
