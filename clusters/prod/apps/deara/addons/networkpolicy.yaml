apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: deara-addon-access
  namespace: app-deara-prod
  annotations:
    platform.deara.ai/temporary-exception: infra/postgres:5432,clawbot/litellm-svc:4000,app-clawhost-prod/clawhost-prod:18080
spec:
  podSelector:
    matchLabels:
      app.kubernetes.io/name: deara
      app.kubernetes.io/instance: deara-prod
  policyTypes:
    - Egress
  egress:
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: infra
      ports:
        - protocol: TCP
          port: 5432
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: clawbot
      ports:
        - protocol: TCP
          port: 4000
    - to:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: app-clawhost-prod
      ports:
        - protocol: TCP
          port: 18080
